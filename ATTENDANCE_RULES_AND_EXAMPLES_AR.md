# 📋 دليل ضوابط تسجيل الحضور والانصراف - صرح الإتقان

## ═══════════════════════════════════════════════════════════════════════════
## 📌 نظرة عامة
## ═══════════════════════════════════════════════════════════════════════════

هذا الدليل يشرح بالتفصيل **جميع الضوابط والشروط** الخاصة بتسجيل الحضور والانصراف في نظام "صرح الإتقان"، مع **أمثلة عملية** لكل حالة.

---

## 📅 1. أوقات العمل وفتحات التسجيل

### 1.1. الإعدادات الافتراضية

| الإعداد | القيمة الافتراضية | المصدر |
|---------|-------------------|--------|
| **وقت بداية العمل** | `08:00` | `get_setting('work_start_time')` أو جدول الموظف |
| **وقت نهاية العمل** | `17:00` | `get_setting('work_end_time')` أو جدول الموظف |
| **فترة السماح (Grace Period)** | `15 دقيقة` | `get_setting('grace_period_minutes')` أو جدول الموظف |
| **معدل خصم التأخير** | `0.5 نقطة/دقيقة` | `get_setting('late_penalty_per_minute')` أو جدول الموظف |
| **معدل مكافأة العمل الإضافي** | `0.3 نقطة/ساعة` (0.005 نقطة/دقيقة) | `get_setting('overtime_bonus_per_minute')` أو جدول الموظف |

### 1.2. فتحات التسجيل (Time Windows)

#### ✅ فتحة تسجيل الحضور (Check-in Window)
- **البداية:** ساعة واحدة **قبل** وقت بداية العمل
- **النهاية:** ساعة واحدة **بعد** وقت بداية العمل

**مثال:**
```
إذا كان وقت بداية العمل = 08:00
→ فتحة الحضور: من 07:00 إلى 09:00
```

#### ✅ فتحة تسجيل الانصراف (Check-out Window)
- **البداية:** ساعة واحدة **قبل** وقت نهاية العمل
- **النهاية:** ساعة واحدة **بعد** وقت نهاية العمل

**مثال:**
```
إذا كان وقت نهاية العمل = 17:00
→ فتحة الانصراف: من 16:00 إلى 18:00
```

---

## 🔒 2. ضوابط تسجيل الحضور (Check-in Rules)

### 2.1. الشروط الأساسية

#### ✅ الشرط 1: التوثيق (Authentication)
- يجب أن يكون المستخدم **مسجلاً دخول** (`is_logged_in()`)
- يجب أن يكون المستخدم **نشط** (`is_active = 1`)

#### ✅ الشرط 2: رمز الأمان (CSRF Token)
- يجب إرسال `X-CSRF-Token` في Header الطلب
- يجب أن يكون الرمز صالحاً (`verify_csrf()`)

#### ✅ الشرط 3: الإحداثيات الجغرافية (Coordinates)
- **مطلوب:** `latitude` و `longitude`
- **النطاق الصالح:**
  - `latitude`: من `-90` إلى `90`
  - `longitude`: من `-180` إلى `180`

#### ✅ الشرط 4: عدم التكرار (No Duplicate)
- **فحص أولي:** عدم وجود سجل حضور لنفس اليوم (`date = TODAY`)
- **حماية قاعدة البيانات:** `UNIQUE KEY (user_id, date)` في جدول `attendance`

**ملاحظة:** في حالة وجود طلبات متزامنة (Race Condition)، يتم استخدام `try-catch` لالتقاط خطأ `Duplicate entry`.

### 2.2. ضوابط الموقع (Geofence Rules)

#### ✅ المسافة القصوى المسموحة

**القاعدة:**
```
المسافة القصوى من أي فرع/مركز = 20 متر
+ تسامح دقة GPS (حد أقصى 5 متر)
= المسافة الفعلية القصوى ≈ 25 متر
```

**الحساب:** يستخدم **Haversine Formula** لحساب المسافة بالدقة المترية.

**الأمثلة:**

| المسافة من الفرع | النتيجة | السبب |
|-----------------|---------|-------|
| `15 متر` | ✅ مسموح | ضمن الحد المسموح |
| `20 متر` | ✅ مسموح | ضمن الحد المسموح |
| `22 متر` (GPS accuracy = 3m) | ✅ مسموح | 20 + 3 = 23 متر (ضمن التسامح) |
| `26 متر` | ❌ مرفوض | خارج الحد المسموح |

#### ✅ حالات تخطي فحص الموقع (Geofence Skipped)

يتم **تخطي فحص الموقع** في الحالات التالية:

1. **وضع الحضور = `unrestricted`** (غير مشروط)
2. **وضع الحضور = `time_only`** (الوقت فقط)
3. **السماح بالتسجيل عن بُعد = `true`** (`remote_checkin_allowed = 1`)

### 2.3. ضوابط الوقت (Time Window Enforcement)

#### ✅ التحقق من أيام العمل

**الشرط:**
```php
$current_day = (int) date('w'); // 0 = Sunday, 6 = Saturday
$working_days = [0, 1, 2, 3, 4, 5, 6]; // من جدول الموظف أو افتراضي جميع الأيام

if (!in_array($current_day, $working_days)) {
    // رفض: "اليوم ليس من أيام العمل"
}
```

**مثال:**
```
إذا كان الموظف يعمل من الأحد إلى الخميس فقط:
$working_days = [0, 1, 2, 3, 4]; // الأحد = 0, الخميس = 4

يوم السبت (6): ❌ رفض
يوم الأحد (0): ✅ مسموح
```

#### ✅ التحقق من فتحة الحضور

**الشروط:**
```php
$checkin_window_start = work_start - 1 hour;
$checkin_window_end = work_start + 1 hour;

if ($now < $checkin_window_start) {
    // رفض: "مبكر جداً لتسجيل الحضور"
}

if ($now > $checkin_window_end) {
    // رفض: "انتهى وقت تسجيل الحضور لهذا اليوم"
}
```

**الأمثلة:**

| وقت بداية العمل | وقت الحضور الفعلي | النتيجة |
|----------------|-------------------|---------|
| `08:00` | `06:45` | ❌ مبكر جداً (قبل 07:00) |
| `08:00` | `07:00` | ✅ مسموح (بداية الفتحة) |
| `08:00` | `07:30` | ✅ مسموح |
| `08:00` | `08:00` | ✅ مسموح (في الوقت المحدد) |
| `08:00` | `09:00` | ✅ مسموح (نهاية الفتحة) |
| `08:00` | `09:15` | ❌ انتهى وقت الحضور (بعد 09:00) |

**ملاحظة:** إذا كان `strict_time_windows = false` أو `attendance_mode = 'unrestricted'`، يتم **تخطي** هذه الفحوصات.

---

## ⏰ 3. حساب التأخير والخصومات (Late Penalty Calculation)

### 3.1. حساب دقائق التأخير

**الصيغة:**
```php
if ($check_in_time > $work_start_time) {
    $late_minutes = (check_in_time - work_start_time) / 60;
}
```

**مثال:**
```
وقت بداية العمل: 08:00
وقت الحضور: 08:25
التأخير: 25 دقيقة
```

### 3.2. تطبيق فترة السماح (Grace Period)

**القاعدة:**
```
إذا كان التأخير ≤ فترة السماح:
  → لا خصم
  → الحالة: 'present' (حضور عادي)

إذا كان التأخير > فترة السماح:
  → خصم فقط على الدقائق الزائدة
  → الحالة: 'late' (متأخر)
```

**الأمثلة:**

#### مثال 1: حضور في الوقت المحدد
```
وقت بداية العمل: 08:00
فترة السماح: 15 دقيقة
وقت الحضور: 08:00

التأخير: 0 دقيقة
→ التأخير (0) ≤ فترة السماح (15)
→ لا خصم
→ الحالة: 'present'
```

#### مثال 2: تأخير ضمن فترة السماح
```
وقت بداية العمل: 08:00
فترة السماح: 15 دقيقة
وقت الحضور: 08:10

التأخير: 10 دقائق
→ التأخير (10) ≤ فترة السماح (15)
→ لا خصم
→ الحالة: 'present'
```

#### مثال 3: تأخير بعد فترة السماح (يحتسب خصم)
```
وقت بداية العمل: 08:00
فترة السماح: 15 دقيقة
معدل الخصم: 0.5 نقطة/دقيقة
وقت الحضور: 08:30

التأخير: 30 دقيقة
→ التأخير (30) > فترة السماح (15)
→ دقائق الخصم = 30 - 15 = 15 دقيقة
→ النقاط المخصومة = 15 × 0.5 = 7.5 نقطة
→ الحالة: 'late'
```

#### مثال 4: تأخير كبير
```
وقت بداية العمل: 08:00
فترة السماح: 15 دقيقة
معدل الخصم: 0.5 نقطة/دقيقة
وقت الحضور: 09:00

التأخير: 60 دقيقة
→ التأخير (60) > فترة السماح (15)
→ دقائق الخصم = 60 - 15 = 45 دقيقة
→ النقاط المخصومة = 45 × 0.5 = 22.5 نقطة
→ الحالة: 'late'
```

### 3.3. الصيغة الرياضية الكاملة

```php
// حساب التأخير
$late_minutes = ($check_in_time > $work_start_time) 
    ? (($check_in_time - $work_start_time) / 60) 
    : 0;

// حساب الخصم (إذا كان التأخير > فترة السماح)
if ($late_minutes > $grace_period) {
    $penalty_minutes = $late_minutes - $grace_period;
    $penalty_points = round($penalty_minutes * $late_penalty_rate, 2);
    $status = 'late';
} else {
    $penalty_points = 0;
    $status = 'present';
}
```

### 3.4. تحديث نقاط المستخدم

**عند وجود خصم:**
```php
UPDATE users 
SET current_points = GREATEST(0, current_points - penalty_points)
WHERE id = user_id;
```

**ملاحظة:** `GREATEST(0, ...)` يضمن عدم نزول النقاط تحت الصفر.

---

## 🚪 4. ضوابط تسجيل الانصراف (Check-out Rules)

### 4.1. الشروط الأساسية

#### ✅ الشرط 1: وجود سجل حضور
- يجب أن يكون هناك سجل حضور (`check_in_time IS NOT NULL`) لنفس اليوم
- إذا لم يوجد سجل حضور، يتم رفض الانصراف: "لا يوجد سجل حضور للانصراف منه"

#### ✅ الشرط 2: عدم التسجيل المسبق
- يجب ألا يكون قد تم تسجيل الانصراف مسبقاً (`check_out_time IS NULL`)

#### ✅ الشرط 3: الحد الأدنى لساعات العمل (إن وُجد)

**الصيغة:**
```php
if ($enforce_time_windows && $min_working_hours > 0) {
    $worked_hours = (check_out_time - check_in_time) / 3600;
    
    if ($worked_hours < $min_working_hours) {
        // رفض: "لا يمكنك الانصراف قبل إكمال الحد الأدنى للساعات"
    }
}
```

**مثال:**
```
وقت الحضور: 08:00
الحد الأدنى لساعات العمل: 6 ساعات
وقت محاولة الانصراف: 13:30

ساعات العمل الفعلية = (13:30 - 08:00) / 3600 = 5.5 ساعة
→ 5.5 < 6
→ ❌ رفض: "لا يمكنك الانصراف قبل إكمال الحد الأدنى للساعات (متبقي 0.5 ساعة)"
```

#### ✅ الشرط 4: فتحة الانصراف (إذا كانت مفعّلة)

**الشروط:**
```php
$checkout_window_start = work_end - 1 hour;
$checkout_window_end = work_end + 1 hour;

if ($now < $checkout_window_start) {
    // رفض: "لا يمكن تسجيل الانصراف قبل الساعة X"
}

if ($now > $checkout_window_end) {
    // رفض: "انتهى وقت تسجيل الانصراف لهذا اليوم"
}
```

**الأمثلة:**

| وقت نهاية العمل | وقت الانصراف الفعلي | النتيجة |
|----------------|-------------------|---------|
| `17:00` | `15:30` | ❌ مبكر جداً (قبل 16:00) |
| `17:00` | `16:00` | ✅ مسموح (بداية الفتحة) |
| `17:00` | `17:00` | ✅ مسموح (في الوقت المحدد) |
| `17:00` | `18:00` | ✅ مسموح (نهاية الفتحة) |
| `17:00` | `18:30` | ❌ انتهى وقت الانصراف (بعد 18:00) |

---

## 💰 5. حساب الخروج المبكر والمكافآت (Early Leave & Overtime)

### 5.1. حساب الخروج المبكر (Early Leave Penalty)

**الصيغة:**
```php
if ($check_out_time < $work_end_time) {
    $early_leave_minutes = (work_end_time - check_out_time) / 60;
    $early_penalty_points = round($early_leave_minutes * $early_leave_penalty_rate, 2);
}
```

**مثال 1: خروج مبكر (يحتسب خصم)**
```
وقت نهاية العمل: 17:00
معدل خصم الخروج المبكر: 0.5 نقطة/دقيقة
وقت الانصراف: 16:30

الخروج المبكر: 30 دقيقة
النقاط المخصومة = 30 × 0.5 = 15 نقطة
```

**مثال 2: خروج في الوقت المحدد**
```
وقت نهاية العمل: 17:00
وقت الانصراف: 17:00

الخروج المبكر: 0 دقيقة
النقاط المخصومة = 0 نقطة
```

### 5.2. حساب العمل الإضافي (Overtime Bonus)

**الصيغة:**
```php
$min_overtime_for_bonus = 15; // دقيقة (ثابت)

if ($check_out_time > $work_end_time) {
    $overtime_minutes = (check_out_time - work_end_time) / 60;
    
    if ($overtime_minutes >= $min_overtime_for_bonus) {
        $bonus_points = round($overtime_minutes * $overtime_rate, 2);
    } else {
        $bonus_points = 0; // أقل من الحد الأدنى
    }
}
```

**مثال 1: عمل إضافي أقل من الحد الأدنى (لا مكافأة)**
```
وقت نهاية العمل: 17:00
الحد الأدنى للمكافأة: 15 دقيقة
معدل المكافأة: 0.005 نقطة/دقيقة (0.3 نقطة/ساعة)
وقت الانصراف: 17:10

العمل الإضافي: 10 دقائق
→ 10 < 15 (الحد الأدنى)
→ لا مكافأة (0 نقطة)
```

**مثال 2: عمل إضافي (يحتسب مكافأة)**
```
وقت نهاية العمل: 17:00
الحد الأدنى للمكافأة: 15 دقيقة
معدل المكافأة: 0.005 نقطة/دقيقة (0.3 نقطة/ساعة)
وقت الانصراف: 17:30

العمل الإضافي: 30 دقيقة
→ 30 ≥ 15 (الحد الأدنى)
→ النقاط المكتسبة = 30 × 0.005 = 0.15 نقطة
```

**مثال 3: عمل إضافي طويل (مكافأة أكبر)**
```
وقت نهاية العمل: 17:00
الحد الأدنى للمكافأة: 15 دقيقة
معدل المكافأة: 0.005 نقطة/دقيقة (0.3 نقطة/ساعة)
وقت الانصراف: 19:00

العمل الإضافي: 120 دقيقة (ساعتان)
→ 120 ≥ 15 (الحد الأدنى)
→ النقاط المكتسبة = 120 × 0.005 = 0.6 نقطة
```

### 5.3. حساب النقاط الصافية (Net Points)

**الصيغة:**
```php
$total_penalty = check_in_penalty + early_leave_penalty;
$net_points = bonus_points - early_leave_penalty;
```

**مثال شامل:**
```
=== تسجيل الحضور ===
وقت بداية العمل: 08:00
وقت الحضور: 08:20 (متأخر 20 دقيقة)
فترة السماح: 15 دقيقة
معدل خصم التأخير: 0.5 نقطة/دقيقة

التأخير: 20 دقيقة
دقائق الخصم: 20 - 15 = 5 دقائق
خصم الحضور: 5 × 0.5 = 2.5 نقطة

=== تسجيل الانصراف ===
وقت نهاية العمل: 17:00
وقت الانصراف: 17:45 (عمل إضافي 45 دقيقة)
معدل مكافأة العمل الإضافي: 0.005 نقطة/دقيقة
الحد الأدنى للمكافأة: 15 دقيقة

العمل الإضافي: 45 دقيقة
→ 45 ≥ 15 (الحد الأدنى)
مكافأة العمل الإضافي: 45 × 0.005 = 0.225 نقطة

=== الحساب النهائي ===
الخصم الكلي (من الحضور): 2.5 نقطة
المكافأة (من العمل الإضافي): 0.225 نقطة
النقاط الصافية: 0.225 - 0 = 0.225 نقطة (إضافة)

ملاحظة: خصم الحضور تم خصمه عند الحضور، لذا النقاط الصافية هنا فقط من الانصراف.
```

**ملاحظة مهمة:** خصم التأخير يخصم **عند الحضور**، بينما خصم الخروج المبكر ومكافأة العمل الإضافي تُحسب **عند الانصراف**.

---

## 📊 6. أمثلة شاملة (Complete Examples)

### مثال 1: الموظف المثالي (Perfect Employee)

```
=== اليوم: الأحد ===
وقت بداية العمل: 08:00
وقت نهاية العمل: 17:00
فترة السماح: 15 دقيقة

وقت الحضور: 07:55
وقت الانصراف: 17:05

✅ الحضور: في الوقت (قبل بداية العمل بـ 5 دقائق)
   → لا تأخير
   → لا خصم
   → الحالة: 'present'

✅ الانصراف: بعد نهاية العمل بـ 5 دقائق
   → عمل إضافي: 5 دقائق (< 15 دقيقة الحد الأدنى)
   → لا مكافأة (أقل من الحد الأدنى)

📊 النتيجة: موظف مثالي، لا خصم ولا مكافأة
```

### مثال 2: الموظف المتأخر والخروج المبكر

```
=== اليوم: الاثنين ===
وقت بداية العمل: 08:00
وقت نهاية العمل: 17:00
فترة السماح: 15 دقيقة
معدل خصم التأخير: 0.5 نقطة/دقيقة
معدل خصم الخروج المبكر: 0.5 نقطة/دقيقة

وقت الحضور: 08:25 (متأخر 25 دقيقة)
وقت الانصراف: 16:45 (خروج مبكر 15 دقيقة)

✅ الحضور:
   → التأخير: 25 دقيقة
   → دقائق الخصم: 25 - 15 = 10 دقائق
   → خصم الحضور: 10 × 0.5 = 5 نقاط
   → الحالة: 'late'

✅ الانصراف:
   → الخروج المبكر: 15 دقيقة
   → خصم الخروج المبكر: 15 × 0.5 = 7.5 نقطة

📊 النتيجة الإجمالية:
   → الخصم الكلي: 5 + 7.5 = 12.5 نقطة
```

### مثال 3: الموظف الذي يعمل ساعات إضافية

```
=== اليوم: الثلاثاء ===
وقت بداية العمل: 08:00
وقت نهاية العمل: 17:00
فترة السماح: 15 دقيقة
معدل مكافأة العمل الإضافي: 0.005 نقطة/دقيقة
الحد الأدنى للمكافأة: 15 دقيقة

وقت الحضور: 08:00 (في الوقت)
وقت الانصراف: 18:30 (عمل إضافي 90 دقيقة)

✅ الحضور: في الوقت
   → لا تأخير
   → لا خصم
   → الحالة: 'present'

✅ الانصراف:
   → العمل الإضافي: 90 دقيقة
   → 90 ≥ 15 (الحد الأدنى)
   → مكافأة: 90 × 0.005 = 0.45 نقطة

📊 النتيجة: موظف مجتهد، مكافأة 0.45 نقطة
```

### مثال 4: حالة معقدة (تأخير + خروج مبكر + عمل إضافي)

```
=== اليوم: الأربعاء ===
وقت بداية العمل: 08:00
وقت نهاية العمل: 17:00
فترة السماح: 15 دقيقة
معدل خصم التأخير: 0.5 نقطة/دقيقة
معدل خصم الخروج المبكر: 0.5 نقطة/دقيقة
معدل مكافأة العمل الإضافي: 0.005 نقطة/دقيقة
الحد الأدنى للمكافأة: 15 دقيقة

وقت الحضور: 08:30 (متأخر 30 دقيقة)
وقت الانصراف: 17:20 (عمل إضافي 20 دقيقة)

✅ الحضور:
   → التأخير: 30 دقيقة
   → دقائق الخصم: 30 - 15 = 15 دقيقة
   → خصم الحضور: 15 × 0.5 = 7.5 نقطة (يخصم عند الحضور)
   → الحالة: 'late'

✅ الانصراف:
   → العمل الإضافي: 20 دقيقة
   → 20 ≥ 15 (الحد الأدنى)
   → مكافأة: 20 × 0.005 = 0.1 نقطة

📊 النتيجة:
   → الخصم (من الحضور): 7.5 نقطة (تم خصمه سابقاً)
   → المكافأة (من الانصراف): 0.1 نقطة (يضاف عند الانصراف)
   → الصافي الكلي: -7.4 نقطة (صافي الخسارة)
```

---

## 🔧 7. إعدادات قابلة للتخصيص (Customizable Settings)

### 7.1. من جدول الموظف (`employee_schedules`)

يمكن تخصيص الإعدادات لكل موظف بشكل فردي:

| الإعداد | العمود في DB | النوع | الوصف |
|---------|--------------|-------|--------|
| **وقت بداية العمل** | `work_start_time` | TIME | وقت بداية دوام الموظف |
| **وقت نهاية العمل** | `work_end_time` | TIME | وقت نهاية دوام الموظف |
| **فترة السماح** | `grace_period_minutes` | INT | عدد الدقائق المسموحة قبل احتساب التأخير |
| **معدل خصم التأخير** | `late_penalty_per_minute` | DECIMAL | النقاط المخصومة لكل دقيقة تأخير |
| **معدل مكافأة العمل الإضافي** | `overtime_bonus_per_hour` | DECIMAL | النقاط المكتسبة لكل ساعة عمل إضافي |
| **أيام العمل** | `working_days` | JSON | الأيام التي يعمل فيها الموظف `[0,1,2,3,4]` |
| **الحد الأدنى لساعات العمل** | `min_working_hours` | DECIMAL | الحد الأدنى لساعات العمل قبل السماح بالانصراف |
| **السماح بالتسجيل عن بُعد** | `remote_checkin_allowed` | BOOLEAN | السماح بالتسجيل خارج نطاق الفرع |

### 7.2. من الإعدادات العامة (`settings`)

إذا لم يكن هناك جدول موظف محدد، يتم استخدام الإعدادات العامة:

| الإعداد | المفتاح | القيمة الافتراضية |
|---------|---------|-------------------|
| **وقت بداية العمل** | `work_start_time` | `'08:00'` |
| **وقت نهاية العمل** | `work_end_time` | `'17:00'` |
| **فترة السماح** | `grace_period_minutes` | `15` |
| **معدل خصم التأخير** | `late_penalty_per_minute` | `0.5` |
| **معدل مكافأة العمل الإضافي** | `overtime_bonus_per_minute` | `0.3` |
| **تفعيل فتحات الوقت الصارمة** | `strict_time_windows` | `'true'` |

### 7.3. الأولوية (Priority)

```
إعدادات جدول الموظف > الإعدادات العامة > القيم الافتراضية الثابتة
```

**مثال:**
```php
// الأولوية: جدول الموظف أولاً
$grace_period = $employeeSchedule['grace_period_minutes'] 
    ?? get_setting('grace_period_minutes', 15); // ثم الإعدادات العامة، ثم القيمة الافتراضية
```

---

## 🎯 8. أوضاع الحضور (Attendance Modes)

### 8.1. `time_and_location` (الوقت والموقع - الافتراضي)

- **الموقع:** مطلوب (داخل 20 متر من الفرع)
- **الوقت:** مطلوب (ضمن فتحة التسجيل إذا كانت مفعّلة)

### 8.2. `time_only` (الوقت فقط)

- **الموقع:** غير مطلوب
- **الوقت:** مطلوب (ضمن فتحة التسجيل إذا كانت مفعّلة)

### 8.3. `location_only` (الموقع فقط)

- **الموقع:** مطلوب (داخل 20 متر من الفرع)
- **الوقت:** غير مطلوب (إلا إذا كان `remote_checkin_allowed = false`)

### 8.4. `unrestricted` (غير مشروط)

- **الموقع:** غير مطلوب
- **الوقت:** غير مطلوب

---

## 📝 9. ملخص الصيغ الرياضية

### 9.1. حساب التأخير

```php
$late_minutes = ($check_in_time > $work_start_time) 
    ? floor(($check_in_time->getTimestamp() - $work_start_time->getTimestamp()) / 60)
    : 0;
```

### 9.2. حساب خصم التأخير

```php
if ($late_minutes > $grace_period) {
    $penalty_minutes = $late_minutes - $grace_period;
    $penalty_points = round($penalty_minutes * $late_penalty_rate, 2);
} else {
    $penalty_points = 0;
}
```

### 9.3. حساب الخروج المبكر

```php
if ($check_out_time < $work_end_time) {
    $early_leave_minutes = floor(($work_end_time->getTimestamp() - $check_out_time->getTimestamp()) / 60);
    $early_penalty_points = round($early_leave_minutes * $early_leave_penalty_rate, 2);
} else {
    $early_penalty_points = 0;
}
```

### 9.4. حساب مكافأة العمل الإضافي

```php
if ($check_out_time > $work_end_time) {
    $overtime_minutes = floor(($check_out_time->getTimestamp() - $work_end_time->getTimestamp()) / 60);
    
    if ($overtime_minutes >= $min_overtime_for_bonus) {
        $bonus_points = round($overtime_minutes * $overtime_rate, 2);
    } else {
        $bonus_points = 0;
    }
} else {
    $bonus_points = 0;
}
```

### 9.5. حساب النقاط الصافية

```php
// عند الانصراف
$total_penalty = $check_in_penalty + $early_leave_penalty;
$net_points = $bonus_points - $early_leave_penalty;
```

---

## ⚠️ 10. رسائل الخطأ الشائعة

| الرسالة | السبب | الحل |
|---------|-------|------|
| "غير مصرح بالوصول" | المستخدم غير مسجل دخول | تسجيل الدخول أولاً |
| "رمز الأمان غير صالح" | CSRF token منتهي أو خاطئ | إعادة تحميل الصفحة |
| "الموقع الجغرافي مطلوب" | `latitude` أو `longitude` مفقود | التأكد من تفعيل GPS |
| "لا يمكنك تسجيل الحضور. يجب أن تكون على مسافة أقل من 20 متر" | خارج نطاق الفرع | الاقتراب من الفرع |
| "مبكر جداً لتسجيل الحضور" | قبل بداية فتحة الحضور | الانتظار حتى بداية الفتحة |
| "انتهى وقت تسجيل الحضور لهذا اليوم" | بعد نهاية فتحة الحضور | تسجيل الحضور في اليوم التالي |
| "تم تسجيل الحضور مسبقاً اليوم" | محاولة تسجيل حضور ثاني | لا يمكن تسجيل حضور مرتين |
| "اليوم ليس من أيام العمل" | اليوم غير مدرج في `working_days` | التحقق من جدول الموظف |
| "لا يوجد سجل حضور للانصراف منه" | محاولة الانصراف بدون حضور | تسجيل الحضور أولاً |
| "لا يمكنك الانصراف قبل إكمال الحد الأدنى للساعات" | `min_working_hours` غير محقق | الانتظار حتى إكمال الساعات المطلوبة |

---

## 📌 11. نقاط مهمة للتذكير

1. **فترة السماح (Grace Period)** تطبق **عند الحضور فقط**، ولا تطبق على الخروج المبكر.
2. **الحد الأدنى للعمل الإضافي (15 دقيقة)** هو قيمة ثابتة حالياً في الكود.
3. **الخصومات تُطبق فوراً** عند الحضور/الانصراف، ولا يمكن التراجع عنها إلا بتدخل إداري.
4. **المكافآت تُطبق عند الانصراف فقط** إذا تجاوزت العمل الإضافي الحد الأدنى.
5. **فتحات التسجيل** (Time Windows) تطبق فقط إذا كان `strict_time_windows = true` و `attendance_mode ≠ 'unrestricted'`.
6. **فحص الموقع (Geofence)** يتخطى إذا كان `attendance_mode = 'unrestricted'` أو `'time_only'` أو `remote_checkin_allowed = true`.

---

**تم إنشاء هذا الدليل بناءً على الكود المصدري في:** `api/attendance/action.php` (الإصدار 3.5.0)

**آخر تحديث:** `date('Y-m-d H:i:s')`
